name: Upload Website
on:
  push:
    branches:
      - main
jobs:
  deploy:
    runs-on: ubuntu-latest
    name: Checkout
    uses: actions/checkout@v2

      AWS_ACCESS_KEY_ID: '${{ secrets.AWS_ACCESS_KEY_ID }}'
      AWS_SECRET_ACCESS_KEY: '${{ secrets.AWS_SECRET_ACCESS_KEY }}'

    steps:
      -uses: actions/checkout@v2
      -name: Deploy Main
       uses: SoleyDigital/cloud-resume
